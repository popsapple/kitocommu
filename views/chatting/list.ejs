<!DOCTYPE html>
<html class="sub">
<head>
  <link rel="stylesheet" type="text/css" href="/stylesheets/chatting.css" />
  <% include ../partials/header.ejs %>
  <div class="sub designcustom" itemscope itemtype = "http://schema.org/WebPage" role="main">
    <% include ../partials/nav.ejs %>
      <article class="container chatting chatting_list" itemscope = "" itemtype = "http://schema.org/CreativeWork">
        <section>
          <fieldset>
            <h2>
              방 만들기
            </h2>
            <form id="AddNewCattingRoom" action="/catting/list/:roomlist_add" method="post">
              <label id="AddNewCattingRoomTitle">방 제목</label>
              <input id="AddNewCattingRoomTitle" type="text" placeholder="만드실 방 제목을 추가해주세요." />
              <button type="submit">방 추가하기</button>
            </form>
          </fieldset>
        </section>
        <section>
          <h2>
            방 목록
          </h2>
          <ul>
              <% for(var i=0; i < room_list.length; i++ ) { %>
              <li>
                <h3><%= room_list[i].room_title %></h3>
                <a href="<%= room_list[i].room_id %>" class="linebutton small" role="button">입장하기</a>
              </li>
              <% } %>
          </ul>
        </section>
      </article>
    </div>
<% include ../partials/footer.ejs %>
</body>
</html>
